# Optional Wan2.1 text-to-video stack.
# Requires diffusers>=0.33.0, which conflicts with chatterbox-tts (diffusers==0.29.0).
# Use this with VOICE_STACK=coqui or VOICE_STACK=none, not with VOICE_STACK=chatterbox.
diffusers==0.34.0
transformers>=4.57.0,<5.0
torch==2.6.0
accelerate>=1.5.0
ftfy
imageio
imageio-ffmpeg
setuptools<70
# Keep the base ASGI stack pinned so gradio/chatterbox do not override it
fastapi==0.115.6
pydantic>=2.10.4,<3.0.0
uvicorn[standard]==0.34.0
