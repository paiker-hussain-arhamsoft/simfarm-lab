# Optional Coqui XTTS v2 voice stack.
# Coqui and Chatterbox cannot be installed in the same environment because they
# require different major versions of `transformers`; install this file *or*
# requirements-voice.txt, not both.
coqui-tts==0.27.5
torch==2.6.0
torchaudio==2.6.0
transformers>=4.57,<5.0
setuptools<70
# Keep the base ASGI stack pinned so gradio does not override it
fastapi==0.115.6
pydantic>=2.10.4,<3.0.0
uvicorn[standard]==0.34.0
